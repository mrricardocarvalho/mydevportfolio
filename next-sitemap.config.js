/**
 * next-sitemap configuration
 * Replace `siteUrl` with your production URL after deploying to Vercel
 */

module.exports = {
  siteUrl: 'https://your-domain.com',
  generateRobotsTxt: true,
  changefreq: 'weekly',
  priority: 0.7,
};
